{
  "name": "chronicle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cargo run --manifest-path backend/Cargo.toml",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:release": "npm run build && cargo build --release --manifest-path backend/Cargo.toml",
    "test": "cargo test --manifest-path backend/Cargo.toml",
    "test:frontend": "npm --prefix frontend run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
